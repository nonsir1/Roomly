# Roomly — Система бронирования переговорных комнат

Веб-приложение для управления бронированием переговорных комнат в организации. Позволяет сотрудникам просматривать доступные помещения, бронировать временные слоты и управлять своими бронированиями в удобном интерфейсе, вдохновленном Google Календарем.

**Учебный проект** разработан в рамках курса **«Технологии программирования»**.

---

## 📋 Возможности системы

### Для пользователей
- ✅ **Регистрация и авторизация** с JWT-токенами
- 🔍 **Каталог переговорных** с бесконечной прокруткой и фильтрацией по:
  - Вместимости
  - Наличию удобств (проектор, ТВ, доска и т.д.)
- 📅 **Почасовое бронирование** на сетке времени
- ⏰ **Визуализация занятости** комнат в реальном времени
- 🚫 **Защита от двойного бронирования**
- 📖 **Личный кабинет** со списком всех бронирований
- ✏️ **Редактирование и отмена** своих бронирований

### Для администраторов
- 🛠️ **Управление комнатами**: добавление, редактирование, удаление
- 🎯 **Управление удобствами** (features)
- 👥 **Управление пользователями**: просмотр списка, изменение ролей
- 📊 **Просмотр всех бронирований** с возможностью редактирования
- ⚙️ **Настройки системы**: рабочее время, продолжительность слотов

---

## Архитектура проекта

### Backend (Node.js)

**Расположение:** `backend-node/`

**Технологии:**
- **Node.js** + **Express.js** — серверный фреймворк
- **Prisma ORM** — работа с базой данных
- **SQLite** — легковесная БД для локальной разработки
- **JWT** — авторизация и аутентификация
- **bcryptjs** — хеширование паролей
- **Multer** — обработка загрузки файлов

**API Endpoints:**
- `/api/auth/*` — регистрация, вход, проверка токена
- `/api/rooms/*` — получение списка комнат, деталей комнаты
- `/api/bookings/*` — создание, редактирование, удаление бронирований
- `/api/features/*` — список удобств
- `/api/admin/*` — административные операции
- `/api/settings/*` — настройки системы

**База данных:**

Модели (Prisma Schema):
1. **User** — пользователи (email, passwordHash, role)
2. **Room** — переговорные (name, capacity, description, imageUrl)
3. **Feature** — удобства (name)
4. **Booking** — бронирования (userId, roomId, startTime, endTime, title)
5. **Setting** — настройки (key, value)

### Frontend (React)

**Расположение:** `frontend/`

**Технологии:**
- **React 19** — UI библиотека
- **Vite** — быстрый сборщик
- **React Router** — клиентская маршрутизация
- **Axios** — HTTP-запросы к API
- **SCSS/Sass** — стили (написаны вручную, без UI-библиотек)

**Роутинг:**
- `/` — главная страница с каталогом комнат
- `/login` — вход в систему
- `/register` — регистрация
- `/room/:id` — детальная страница комнаты с сеткой бронирования
- `/my-bookings` — личные бронирования пользователя
- `/admin` — административная панель (только для администраторов)

---

## Инструкция по развертыванию

### Требования
- **Node.js** версии 16 или выше
- **npm** или **yarn**

### 1. Клонирование репозитория

```bash
git clone <URL_репозитория>
cd Roomly
```

### 2. Настройка Backend

#### 2.1. Установка зависимостей

```bash
cd backend-node
npm install
```

#### 2.2. Настройка переменных окружения

Создайте файл `.env` в папке `backend-node/`:

```
DATABASE_URL="file:./prisma/roomly.db"
JWT_SECRET="ваш_секретный_ключ"
JWT_EXPIRES_IN="30m"
PORT=8000
```

#### 2.3. Инициализация базы данных

```bash
npx prisma migrate dev
```

#### 2.4. Запуск сервера разработки

```bash
npm run dev
```

Сервер будет доступен по адресу: **http://localhost:8000**

### 3. Настройка Frontend

#### 3.1. Установка зависимостей

Откройте новый терминал:

```bash
cd frontend
npm install
```

#### 3.2. Запуск сервера разработки

```bash
npm run dev
```

Приложение откроется по адресу: **http://localhost:5173**

### 4. Деплой в продакшн

#### 4.1. Сборка Frontend

```bash
cd frontend
npm run build
```

Статические файлы будут в папке `frontend/dist/`

#### 4.2. Запуск Backend в продакшене

```bash
npm install -g pm2
cd backend-node
pm2 start ecosystem.config.cjs
pm2 save
pm2 startup
```

#### 4.3. Проверка работы

```bash
pm2 status
pm2 logs roomly-backend
```