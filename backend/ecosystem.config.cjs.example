module.exports = {
  apps: [{
    name: 'roomly-backend',
    script: 'src/server.js',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production',
      PORT: 3000,
      DATABASE_URL: 'file:./roomly.db',
      JWT_SECRET: 'key',
      JWT_EXPIRES_IN: '30m'
    }
  }]
};

